node_modules

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
.expo/

# EAS local build artefacts
*.ipa
*.tar.gz

# Apple signing secrets — NEVER commit. The .p8 downloads exactly once and
# cannot be re-downloaded; an account is capped at 2 APNs keys.
*.p8
*.mobileprovision
*.p12

# Native projects — this is a CNG app; `expo prebuild` regenerates these from
# app.json, which is the source of truth. Never commit them.
/ios
/android
