# Decision Log

One file per decision: `NNNN-short-slug.md`, numbered sequentially, never
renumbered. Superseding a decision means writing a **new** entry and marking the
old one `Superseded by 00NN`, not editing history.

## Template

```markdown
# NNNN — <Title>

- **Date:** YYYY-MM-DD
- **Status:** Accepted | Superseded by NNNN | Reversed
- **Decided by:** <who>

## Context
What forced a choice.

## Decision
What we're doing, stated plainly.

## Consequences
What this makes easy, what it makes hard, what it commits us to.

## Alternatives considered
Option — why not.
```

Keep entries short. A decision nobody could reconstruct from the code is worth
more words than one that's obvious once you see it.

## Index

| # | Decision | Status |
| --- | --- | --- |
| [0001](./0001-expo-sdk-57-managed.md) | Expo SDK 57, managed workflow | Accepted |
| [0002](./0002-expo-router-file-based.md) | expo-router with file-based routing | Accepted |
| [0003](./0003-src-directory-and-alias.md) | Code under `src/`, `@/` path alias | Accepted |
| [0004](./0004-typescript-strict.md) | TypeScript with `strict: true` | Accepted |
| [0005](./0005-native-tabs.md) | Native tabs via `expo-router/unstable-native-tabs` | Accepted |
| [0006](./0006-theme-constants-module.md) | Centralized theme constants, no styling library | Accepted |
| [0007](./0007-product-scope-tbd.md) | Product scope not yet defined | Superseded by 0012 |
| [0008](./0008-document-every-decision.md) | Every decision is recorded in this log | Accepted |
| [0009](./0009-backend-is-only-data-gateway.md) | `senpai-backend` is the only data gateway; `cronogol` is the reference implementation | Accepted |
| [0010](./0010-alta-gama-fc-repo-deprecated.md) | `alta-gama-fc` repo is deprecated | Superseded by 0011 |
| [0011](./0011-alta-gama-fc-repo-removed.md) | `alta-gama-fc` repo removed; deny rule dropped | Accepted |
| [0012](./0012-native-ios-port-of-cronogol.md) | Native iOS app porting `cronogol`, for App Store release | Accepted |
| [0013](./0013-atomic-design-components.md) | Atomic design for the component layer | Accepted |
| [0014](./0014-app-specific-design-language.md) | App-specific design language (in progress) | Resolved by 0015 |
| [0015](./0015-handoff-design-system-adopted.md) | Handoff design system adopted; theme module replaced | Accepted |
| [0016](./0016-ios-only-v1.md) | Web target dropped; iOS-only v1 | Accepted |
| [0017](./0017-react-query-data-layer.md) | React Query, with the web app's cache buckets as `staleTime` | Accepted |
| [0018](./0018-ported-data-layer.md) | The ported `lib/cronogol/` layer mirrors the web app path for path | Accepted |
| [0019](./0019-anonymous-v1.md) | Anonymous v1: follows are device-keyed, accounts designed-for | Superseded by 0038 |
| [0020](./0020-root-stack-wrapping-tabs.md) | Root layout is a `Stack` wrapping a `(tabs)` group | Accepted — sheets revised by 0030 |
| [0021](./0021-short-club-display-names.md) | Club names are shortened for display | Accepted |
| [0022](./0022-finished-today-from-fixtures.md) | `FINISHED TODAY` is built from `/cronogol/fixtures`, not the scoreboard | Accepted |
| [0023](./0023-phase-3-resequenced-no-apple-account.md) | Phase 3 resequenced around a pending Apple Developer account | Blocked half closed by 0024 |
| [0024](./0024-push-enabled.md) | Push enabled; Live Activity deferred to v1.1 | Accepted |
| [0025](./0025-widgets-deferred.md) | Widgets deferred; no native targets in the tree yet | Superseded by 0047 |
| [0026](./0026-apns-environment-is-the-provisioning-profile.md) | The APNs environment follows the provisioning profile, not the build name | Accepted |
| [0027](./0027-board-lead-cards-from-fixtures.md) | Today's live and last-result cards read `/cronogol/fixtures`; the scoreboard is not read by the app | Accepted |
| [0028](./0028-matchday-pager.md) | Matchdays pager: one clamped setter, text chevrons, no range while provisional | Accepted |
| [0029](./0029-upcoming-rows-read-from-the-followed-club.md) | `UPCOMING FROM YOUR CLUBS` rows are the crest pairing over `kickoff · day · venue`, with the relative day accented only today | Superseded by 0043 |
| [0030](./0030-sheets-are-presented-by-the-root-stack.md) | The sheets are presented by the ROOT stack, not a `(sheets)` layout | Accepted |
| [0031](./0031-league-filter-tiles-are-artwork-only.md) | The league filter tiles are artwork only, at one fixed size | Accepted — geometry + idle rule Superseded by 0089; artwork-only stands |
| [0032](./0032-clubs-browse-by-league.md) | Clubs browses one league at a time, behind the Matchdays filter row; search stays global | Accepted |
| [0033](./0033-player-detail-sheet.md) | Tapping a squad row opens a player sheet, read from the cached squad payload; a null keeps its label and blanks its cell | Accepted |
| [0034](./0034-next-up-card-live-seconds-countdown.md) | The next-up card leads with its crests and an accent versus ring; its countdown ticks per second, torn down while backgrounded | Accepted |
| [0035](./0035-jornada-rows-show-in-play-scores.md) | Jornada rows: 40pt `v` crest pairing, a clock-split timing column, and an in-play score captioned `In play`, never `LIVE` | Accepted |
| [0036](./0036-app-icon-appearance-variants.md) | The app icon is three 1024s wired through `ios.icon`'s variant object; the tinted variant must be opaque | Superseded by 0041 |
| [0037](./0037-rich-notifications-server-composed-crests.md) | Rich notifications: the backend composes every crest; the app grows a service and a content extension | Accepted — verified on device |
| [0038](./0038-accounts-apple-and-google.md) | Accounts: Apple and Google, sign-in optional | Accepted — email/password clause superseded by 0103; the rest stands |
| [0039](./0039-auth-goes-direct-to-supabase.md) | Auth goes direct to Supabase `/auth/v1`; the backend stays the only *data* gateway | Accepted — waypoint, proxied auth intended |
| [0040](./0040-kickoff-reminder-lead-times.md) | Kickoff reminders offer 1 h / 30 min / 15 min lead times; the notification budget counts notifications, not fixtures | Accepted — not yet on a device |
| [0041](./0041-app-icon-1b-and-a-splash-of-its-own.md) | App icon 1b: three opaque appearance masters, and a splash asset of its own | Icon superseded by 0060; splash arrangement stands |
| [0042](./0042-brand-spelling-spaced-form-reinstated.md) | The spaced `Alta Gama FC` is reinstated | Accepted — open across the ecosystem |
| [0043](./0043-upcoming-cards-name-the-sides.md) | `UPCOMING FROM YOUR CLUBS` is one card per match: the sides named home-lit over away-dim, `MD n · ground` as the eyebrow, kickoff over its date, accent only on today | Accepted |
| [0044](./0044-scores-render-as-split-digits.md) | A score is the `Score` atom — two digits split by a rule, the losing digit dimmed, chipped in a list and bare in the Today hero | Accepted — verified on the simulator |
| [0045](./0045-match-events-expanded-row.md) | A finished match expands into its event timeline: six event types drawn, disclosure gated on status not data, `react-native-svg` adopted | Accepted |
| [0046](./0046-match-events-grouping-tabs.md) | The events panel groups behind four counted tabs — `goals` / `cards` / `subs` / `other` — which replace its eyebrow; the shown group is derived, never stored | Accepted |
| [0047](./0047-widgets.md) | Widgets: both home-screen sizes, the Lock Screen accessories and a per-club picker; the App Group snapshot carries its own copy, and timeline ENTRIES are not reloads | Accepted — verified on the simulator |
| [0048](./0048-live-scores-on-the-today-board.md) | Live scores upgrade Today's in-progress card: `/cronogol/live` gives it a real minute where it can, the ~3h fixture sweep stays the fallback everywhere else, and the scoreboard's suppression is untouched | Accepted — decision 4 revised by 0049 |
| [0049](./0049-stall-is-lastseenat-not-polling.md) | The live card's stall signal is `lastSeenAt` alone; `polling` is not read — production served `polling: false` for a whole match that was updating fine | Accepted |
| [0050](./0050-match-events-on-the-in-progress-card.md) | The in-progress card expands into its timeline, reversing 0045's divergence 1: the chevron returns because that premise expired, `notPublished` is reused rather than given an in-play variant, and a `supplied` seam absorbs the backend's still-open decision on where live events land | Accepted — the panel is empty on real data until the backend lands live events |
| [0051](./0051-live-match-events-inline.md) | Live events arrive inline on `/cronogol/live` and reach the card through 0050's `supplied` seam; one composed `eventKey` serves both sources so the full-time handover does not remount; the durable-route poll is retired | Accepted |
| [0052](./0052-kickoff-triggers-the-live-refetch.md) | The next-up countdown announces kick-off and the board refetches on it — the fixture windows both END at `now`, so a match that starts after they were fetched is in neither and the live poll has nothing to join to; window bounds move into the `queryFn` so a refetch asks for the window as of now | Accepted |
| [0053](./0053-live-match-push-alerts.md) | Live match push alerts: goals, red cards and full time behind ONE `alertGoals` switch that defaults off; the 38pt attachment plate is drawn on device rather than composed by the backend, because a glyph has no upstream format to decode; the meta row is the long-look card, not the banner | Accepted — not yet on a device |
| [0054](./0054-live-activities-still-deferred.md) | Live Activities remain deferred and 0024's call stands — the blockers are now specific (a different APNs topic, a per-Activity token table, Apple's update budget) rather than "no data"; the phase-2 path is written down in `LIVE-ACTIVITIES.md` | Accepted |
| [0056](./0056-onboarding-league-chips-name-themselves.md) | Onboarding browses every league behind a 2-up chip grid that NAMES each one, narrowing 0031 to the filter row; no light plate, because the Premier League's mark is white-for-dark and 0031's faintness was the 50% idle opacity | Accepted — not yet run on the simulator |
| [0055](./0055-live-activities-broadcast-channels.md) | Live Activities ship on iOS 18 broadcast channels and REPLACE the goal banner for devices that have a card; the widget target stays at 17.0 with availability gating rather than raising its floor; push-to-start is a `device_tokens` column, not nothing; the check-age footer is retired and no stall line replaces it | Accepted — shipped dark, never on a device |
| [0057](./0057-local-expo-module.md) | A local Expo module at `modules/live-activity/` is how Swift reaches the APP target — `@bacons/apple-targets` builds extensions only, and the push-to-start token can only be read in the app process; the podspec reaches into `targets/_shared/` to keep `MatchAttributes` at one copy | Accepted |
| [0058](./0058-next-widget-centres-and-ticks.md) | The small NEXT widget centres on 38pt crests, and inside the final 12h its countdown ticks per second via `Text(timerInterval:)` — a SYSTEM-drawn timer that costs zero reloads, so 0025 step 5 still holds; beyond 12h the `1d 14h` string stays, because the system's timer format has no days unit | Accepted — verified on the simulator |
| [0059](./0059-your-week-widget-names-both-sides.md) | The medium YOUR WEEK row names BOTH sides home-first, lights the followed one and tags it `HOME`/`AWAY` — the inverse of 0043's unmarked sides, because a per-club list is about your club; kickoff becomes a stacked `SAT`/`21:00` column; snapshot v2 with every new field optional in Swift so a v1 file degrades instead of blanking; names contract (`R. Madrid`) because two full names and a pill do not fit one row | Superseded by [0086](./0086-week-widget-hero-and-rail.md) — the layout; its home-first and lit-followed-side rules survive |
| [0060](./0060-app-icon-1a-floodlight.md) | App icon 1a "Floodlight": graphite field, lime glow, lime mark on every appearance; same geometry as 1b so the splash and widget mark are untouched; masters verified fully opaque before copying | Accepted — not yet on a device |
| [0061](./0061-news-widget.md) | The NEWS widget (`systemLarge`, design 1a) reads the GLOBAL `/cronogol/news` feed and opens the publisher in Safari; `expo-image-manipulator` encodes one cover-cropped 924×348 JPEG per item into `news/{id}.jpg`; a second App Group file `widget/news.json` with its own prune and change guard; age is derived in Swift from raw `filedAt`; `senpai-backend` untouched | Accepted — verified on the simulator |
| [0062](./0062-league-header-bands.md) | FINISHED TODAY's league headers sit on a per-league brand band from a front-end `LeagueBand` map keyed by API slug — NOT backend `accentColor` (null on Serie A/segunda, no gradient); segunda shares LaLiga's red; Serie A's gradient is drawn with `react-native-svg`, not `expo-linear-gradient`; unlisted slugs keep the neutral header | Accepted — not yet verified on the simulator |
| [0063](./0063-next-up-leads-the-board.md) | NEXT UP is the first card on the Today board, LAST RESULT beneath it — the reader opens the app for the next kickoff, not a result they were already pushed; only the live card outranks it | Accepted — not yet verified on the simulator |
| [0064](./0064-news-card-and-screen.md) | News in the app: a NEWS card on the Today board (three stories, one tap) pushing a root-stack News screen over the GLOBAL feed the widget already caches; link-out sheet names the publisher, phase 1 opens the default browser behind `features/news/open.ts` (phase 2: `expo-web-browser`); "Your clubs" deferred — the wire carries no club slugs; chips = `/news/leagues` ∩ `LEAGUES` in server order; `newsSeenAt` marked on opening the screen | Accepted — verified on the simulator |
| [0065](./0065-starting-xi-builder.md) | The Starting XI builder nests under the club page as a folder route; the entry row enables off the SQUAD (LaLiga + Premier League), placements key on the person id, eleven formations with the spacing rule asserted in code, a sibling `store/starting-xi.ts`, three root-stack sheets, a true-1080 off-screen capture via `react-native-view-shot` + `expo-sharing` + `expo-media-library`, drag-to-swap on gesture-handler, haptics via `expo-haptics`; no club switching | Accepted — harness-proven, not yet on the simulator |
| [0066](./0066-live-card-reads-the-live-route-directly.md) | The Today board's live card is picked from `/cronogol/live` ALONE (tier 0, `routeLive`) with crests from the club catalogue — no fixture window behind it; `BoardLive.fixture` narrows to `BoardFixture`; the countdown fires strictly PAST kickoff because an exclusive `to` on the kickoff millisecond excludes the fixture (measured); `liveMinute` nulls a literal 0; revises 0048 decision 2; goal pushes remain a backend flag | Accepted — harness-proven, not yet seen against a real kickoff |
| [0067](./0067-site-origin-and-contact-link.md) | A small "Contact us" text link closes the account sheet footer, opening `altagamafc.com/{locale}/contact` via `Linking`; `SITE_ORIGIN` is pinned in `lib/cronogol/site.ts` like `FEED_ORIGIN`, never env-derived; future website URLs go there | Accepted — not yet verified on the simulator |
| [0068](./0068-club-colour-wash.md) | A club-colour wash behind the next-up card (home top-left, away bottom-right, dark seam) and the club header, from `colorPrimary`/`colorSecondary` tamed by a pure `lib/cronogol/club-wash.ts` (L 22–30 %, yellow/green ≤ 26 %, S ≥ 45 %; near-black/white → secondary → graphite); no colour on either side → today's card unchanged (all PL and Serie A); chrome on a wash goes white not lime; drawn via a `WashGradient` atom on `react-native-svg` with `useId` ids; Today screen joins `TeamRef` to the catalogue by slug | Accepted — harness-proven (16 assertions), verified on the simulator |
| [0069](./0069-finished-today-stacked-rows.md) | FINISHED TODAY rows become a stacked pair — home over away, one crest and name per line, goals in a fixed right-aligned tabular `Size.goalColumn` behind a hairline, chevron in its own column, the `FT` word and `finishedLabel` prop removed; `scoreEmphasis` mutes the loser's name and goal; the `Score` chip stays on every other card | Accepted — verified on the simulator |
| [0070](./0070-news-front-page.md) | The News screen's first day group is a FRONT PAGE — `frontPagePick` → `NewsLead` (picture under a `WashGradient` scrim, kicker, 3-line `title`, 2-line excerpt; a failed picture keeps the story as a text lead) + up to two `NewsTile`s (16:10) + hairline `NewsRow`s; later days are rows; `N NEW` pill on the first header via `SectionHeader.accessory`, counted over the whole feed from the seen stamp CAPTURED AT OPEN; masthead date; `copy.news.lead` | Accepted — verified on the simulator |
| [0071](./0071-today-news-card-lead-picture.md) | The Today card's lead carries its picture across the top of the card via `NewsLead variant="compact"` (2:1.15 `Size.newsCardLeadRatio`, two-line `headline`, no kicker, no excerpt, no Pressable of its own — `onPress` optional); a failed picture → headline-first at `title3`; the card drops its horizontal padding and the rule under the lead; `Size.newsLead` retired | Accepted — verified on the simulator |
| [0072](./0072-xi-portraits-on-the-board.md) | Starting XI pitch tokens and rail tiles draw the player's portrait when `photoUrl` is set, with the shirt number as a corner badge (`Size.xiBadge` 18 / `xiRailBadge` 14, `Type.xiBadge`); null → the number token, a 404 → the number token via `onError`; the selected ring now draws around a portrait; no toggle, no silhouette, `Size.xiToken` untouched | Accepted — verified on the simulator |
| [0073](./0073-xi-export-render-in-context.md) | Save to Photos calls `MediaLibrary.Asset.create(file://…)` — SDK 57 removed `saveToLibraryAsync` from the root entry and left a throwing stub, which was the whole "could not be rendered" error (found from the device log once the catch logged); the XI capture also passes `useRenderInContext: true` (iOS `drawViewHierarchyInRect` refuses the builder's hierarchy while the export sheet is presented over it); the sheet logs every rejection under `[xi-export]` instead of a bare catch; the host guard rejects `capture-host-not-mounted` on a null ref; `LineupCard` settles in an effect, not during render | Accepted — confirmed by the reader's tap: "Saved to Photos" |
| [0074](./0074-export-receipt-state.md) | After a save the Save button becomes the receipt — `outline` + `Check` + `Saved to Photos`, disabled until size/title/lineup change (route clears the typed `ExportStatus` in its handlers); a `StatusBanner` molecule (lime success / red error / amber warning, `FadeInDown` unless Reduce Motion) replaces the caption; `hapticSaved()` Success notification; `shared` clears the status | Accepted — verified on the simulator |
| [0075](./0075-card-caption-reserve.md) | The export card's `PitchSlots` projects y onto `height − insetBottom`, with `CARD.captionReserve` (120 = ring/2 + nameGap + tallest caption) so the goalkeeper's name is never clipped on 1:1 and 4:5; the board passes no inset and is unchanged | Accepted — verified on the simulator |
| [0076](./0076-onboarding-floodlight-redesign.md) | Onboarding is redesigned around the icon's "Floodlight": a new welcome screen (`Mark` + `Glow` atoms, `StepDots`), 0056's chip grid becomes a horizontal `LeaguePills` row with the same rules, the picker goes 3-up crest-led on a new `pickerName`, the CTA carries a `PickStack` of picked crests (revisiting 0056's rejected strip — same feedback, smaller surface), the primer shows a sample `AlertPreview` and `AlertGlyph` rows; `Button tall`; gate and Replay route to `/onboarding/welcome` | Accepted — not yet run on the simulator |
| [0077](./0077-welcome-language-line-and-one-skip.md) | The welcome screen carries a one-line language override written in the OTHER language (`copy.onboarding.switchLanguage` + `GlobeGlyph`, swaps copy in place via `setLanguage`), chosen over an `EN \| ES` pill because only the reader it is for can see it; the welcome and picker skips are removed — the primer's `Not now` is the flow's only skip, so a reader must follow one club to reach the app | Accepted — not yet run on the simulator |
| [0078](./0078-kicked-off-lead-card.md) | A third lead-card tier, `kickedOff`, between the live route and the sweep: a followed club's `scheduled` fixture whose kickoff passed within 2h30 and which `/cronogol/live` has never served leads as an in-progress card with dashes and a "kicked off · waiting" note, built from windows already on screen (`onKickoff` bumps a reducer so it flips synchronously); `next`/`mine` exclude passed kickoffs; a per-session `seenLive` set stops the card returning after full time and refetches `today`/`recent` when a row vanishes. Because LaLiga flips to in-play at the actual whistle, 2–5 min after the scheduled one (measured 2026-08-30) | Accepted — harness-proven; the flip not yet watched on a device |
| [0079](./0079-push-sync-survives-a-stale-bearer.md) | `register()` in the push sync tries the held bearer, then a refreshed one once, then anonymously (lossless: the route keeps the account link); never signs out; logs every non-sent outcome under `[push-sync]`; a persisted `push-sync-status` store feeds the account sheet's footnote — never registered · saved at time · could not be saved. Because the one live device row read `alert_goals: false` since 2026-08-28 while the switch said on, and three dispatched goals reached zero devices | Accepted — gates clean; re-registration not yet watched on the device |
| [0080](./0080-widgets-live-scores-poll-plus-push.md) | The widgets draw a live ledger (Ed's "Direction B"): `widget/live.json` written by a rationed provider poll of `/cronogol/live` (5-min reloads inside a 150-min match window — the one carve-out from 0025's no-network rule), by goal/red/full-time pushes in the service extension, and by the foregrounded app; per-minute timeline entries tick the minute from a `minuteAt` anchor; HT is a heuristic, FT is the row vanishing, the score holds until midnight; snapshot v3 adds `leagueSlug` (LaLiga-only gate) and the `LIVE`/`HT`/`FT` copy | Accepted — verified on the simulator (live + FT); real match and push writer pending. ⚠ The ledger and the poll are superseded on the MEDIUM widget by [0086](./0086-week-widget-hero-and-rail.md); the small widget is unchanged |
| [0081](./0081-account-sheet-redesign.md) | The account sheet is redesigned: an `Avatar` identity block, per-type `AlertTile`s (`moved` blue / `postponed` amber, the rest neutral), the lead times as chips that are not drawn while the master is off, crests on the feed rows, and the app's first staggered `FadeInDown` behind a new `Motion` token scale; haptics move to `lib/haptics.ts`. Amends 0040's presentation and shares 0076's tile. The mock's custom switch, glows and nested shells were deliberately NOT built — `Switch` stays the platform control and SPEC §2's no-shadow rule stands | Accepted — verified on the simulator, signed-in half still on a stub |
| [0082](./0082-clubs-screen-rail-redesign.md) | The Clubs screen is redesigned: the follow list becomes a horizontal RAIL of 88pt crest bubbles whose only action is opening the club (unfollow stays on the club page, behind a confirm), rank badges for EVERY league whose table passes `bandsApply` and banded by `League.zones` — not the pack's LaLiga-only claim or its `pos<=4` placeholder, both stale — the SAME `LeagueSwitch` the Matchdays and Table tabs draw rather than the pack's bespoke chip row, and one browse tray with a follow pill on `hapticToggle` (the pack's per-row club-colour tick was built and removed — `BandRail` already owns the 3pt vertical rail, where it means a qualification zone). Club colour extends `club-wash.ts` with `clubTint` (no lightness clamp — that one is for white text) plus a 45-entry `TINT_FALLBACK` covering the PL, Serie A, and five clubs served pure black or white; 99 of 100 clubs now carry a colour. The bubble takes BOTH of the pack's depth devices as named, single-component exceptions — a coloured glow against 0015's no-shadow rule and a double bezel (`bezelFill` + a `bezelHighlight` lit arc) against 0081's no-nested-shells rule — while every CARD on the screen stays one flat surface | Accepted — harness-proven (68 assertions), verified on the simulator |
| [0083](./0083-eas-capability-sync-strips-broadcast.md) | EAS capability sync does not merely skip the Push Notifications **Broadcast** sub-feature — it actively turns it OFF on every build, because Broadcast is a nested sub-option (`PUSH_NOTIFICATION_FEATURE_BROADCAST`) and the syncer re-creates `PUSH_NOTIFICATIONS` at defaults ([expo/eas-cli#3815](https://github.com/expo/eas-cli/issues/3815)). It stripped the capability enabled on 2026-08-28, silently, and the first production channel create returned `400 BroadcastFeatureNotEnabled`. Builds now go through `yarn build:development｜preview｜production`, which set `EXPO_NO_CAPABILITY_SYNC=1` in the SHELL — ⚠ it cannot live in `eas.json`'s `env`, which is loaded after capability sync has already run. ⚠ Never run a bare `eas build` again; ⚠ the capability needs re-enabling by hand once more | Accepted — mitigation shipped, portal re-enable outstanding |
| [0084](./0084-widget-live-gate-takes-the-api-league-slug.md) | The widget's live gate compares `leagueSlug` against the **API** slug `laliga`, not our internal `la-liga` — 0080 shipped with the internal one, so `liveEligible` was false for EVERY real fixture, `rows(inPlayAt:)` always returned empty and `/cronogol/live` was never polled: the entire live ledger was dead on the 2026-08-31 preview build (Barcelona v Rayo drew the upcoming layout throughout). Fixed on the reader rather than translating in the writer, so existing `snapshot.json` files repair without a foreground. `WidgetSnapshot.placeholder` and BOTH `_debug/widgets` sample rows move to `laliga` too — the sample hardcoding `la-liga` is what made 0080's simulator verification pass against a broken gate, and a `_debug` sample that invents a field rather than copying the wire cannot test anything | Accepted — confirmed against the real on-disk snapshot; not yet watched through a live kickoff |
| [0085](./0085-broadcast-card-redesign.md) | The Live Activity is redrawn from `handoff_alerts-redo/` as the floodlight "broadcast card" — two states, `EllipticalGradient` floodlights, a scorer band, and the pre-match form/kickoff state that until now had NO LIFETIME because the server only started a card once a fixture was already `live`; it now starts at T−10 inside the window that was already open, which needed a kickoff-aware `stale-date` (an 8-minute one greys the card BEFORE the match it advertises) and a prune that also reaps `postponed`. ⚠⚠ The band carries ONE scorer line a side with an inline `+n`, because the design as drawn MEASURES 203.5pt against Apple's ~160pt cap and every two-row arrangement lands at 159.5pt with zero margin; the obvious crest trim saves exactly nothing, the 46pt crest never binds. The handoff's own `BroadcastActivity.swift` is a design artifact — a second incompatible `MatchAttributes`, `AsyncImage` crests and asset-catalog glyphs that no target has | Accepted — ✅ rendered on a real device (all four states, via the probe); the T−10 start and the stale-date fix are still unproven, because only a real kickoff can exercise them |
| [0086](./0086-week-widget-hero-and-rail.md) | YOUR WEEK is redrawn from `handoff_week-widget/`: one hero kickoff on a floodlit plate, the rest as a rail, and NO wire change — snapshot v3 already carried every field. ⚠⚠ The hero STACKS home over away where the mock draws one row, because the mock's row MEASURES 183pt against a 157pt hero column on an SE and neither trim closes it — dropping the `v` saves 11pt and the crests 20→18 save **4**, since the two names bind, not the crest (0085 §3's lesson, paid once). Stacking spends the 33pt of height the tile was not using and buys the crests 20→24. The shell is a FLUSH plate via the repo's first `.contentMarginsDisabled()`, not the mock's nested tray, which would put a second radius inside the system's own mask. ⚠⚠ The medium tile drops the live ledger AND its poll (gated on `context.family`, so the small widget keeps both) — the cost is that a match in play is simply absent from this tile. Zero new tokens; the handoff's `enum Ink`, `AsyncImage` crests, asset-catalog mark and parallel `WeekSnapshot` are all design artifacts | Superseded by [0127](./0127-medium-tile-day-spine.md) — the medium tile is a day spine; home-first and the lit followed side survive in it |
| [0087](./0087-crown-aurora-shell-adopted.md) | The `handoff_new-paint/` crown + aurora shell is adopted: ground `#0a0b0c → #0f1316` under one static three-pool mesh (`MeshGround`, drawn once per screen behind the scroll), charcoal body cards become GLASS (`Surfaces.glass` — `rgba(255,255,255,.06)` + 0.5pt `.11` hairline), the match-events recession becomes translucent `recess` (`sunken` is invisible on the new ground), sheets go opaque `sheetGround #101316`, and the crown ink arrives as its own `onCrown*` family — deliberately NOT `onAccent`, which stays the on-lime-control ink copied into Swift. ⚠ Add-don't-mutate: `card` keeps `#15171a` because it feeds SVG stops (trap 42) and washed cards need an opaque base. Mockup-precedence rule recorded with four carve-outs (League.zones, UISwitch, honesty lines, 0063's NEXT-UP-first). Crowns/chips/bubbles/club-page/news land as 0088–0093 | Accepted — P1 shipped; tsc + lint clean, simulator pass pending |
| [0088](./0088-live-match-is-the-crown-payload.md) | Today's live match becomes the CROWN's payload — `organisms/live-plate.tsx` is `match-board`'s live branch extracted whole (data contract and every warning verbatim; only the surface changed: `plateDark` glass, lit top edge, events on `recess`, red border gone — liveness is the pill + minute). `MatchBoard` keeps next/last and renders only when nothing is live, preserving the never-both invariant at the call site; `onKickoff`/kicked-off tier/`useLive` polling untouched; `EventsDisclosure` promoted to `molecules/` on its second consumer. The washed next-up card moves to the 100° `ClubWash2` pair wash over an opaque `card` base | Accepted — verified on the simulator via `/_debug/gallery?only=live`; a real live match pending |
| [0089](./0089-league-chips-grayscale-inactive.md) | League chips: 36pt mark-hugging chips replace 0031's 80×56 tiles (artwork-only rule survives), with a `tone: crown\|ground` pair of palettes; INACTIVE marks are true-grayscaled via `react-native-svg/filter-image` (`feColorMatrix saturate 0`, native filter already in the build, per-instance ids); SELECTED inverts to a near-black plate with the mark back in full colour — the one double inversion. ⚠ An SVG mark (Serie A's `primary`) renders EMPTY through FilterImage's bitmap-only `Image` — found on the simulator; SVG sources keep expo-image at .45 opacity, a recorded divergence keyed on the URL so it self-heals if a bitmap cut ever ships | Accepted — verified on the simulator |
| [0090](./0090-clubs-bubbles-liquid-glass-and-trays.md) | The Clubs rail bubble goes LIQUID GLASS: an `expo-glass-effect` `GlassView` where `isLiquidGlassAvailable()` (its first use in the repo; gate decided at module scope, before mount) over a flat `bubbleShell` floor elsewhere, both sharing one overlay stack (rim, `BubbleGlass` wash, bounce, elliptical specular pool — the mock's blurred arc read as a decal, recorded divergence). Club-colour ring/radial/glow retired; the check gains a white rim; the rank pill goes FROSTED with `onCrown` ink and the band colour as a leading 4pt dot (band inks fail contrast on white); `ClubBubble` loses `tint`. `molecules/tray.tsx` is the double bezel as a component (inner radius ALWAYS outer − trayPad) — the search field (r24) and `ClubBrowser` (r26) are its first homes; `ChipButton`'s ring goes 1pt (hairline lime vanished on the mesh). 0082's behaviour — one action, `bandsApply`/`League.zones` ranks, unfollow-not-here — survives whole | Accepted — verified on the simulator |
| [0091](./0091-club-page-hero-and-trays.md) | The club page is rebuilt from `handoff_new-paint/`: `club-hero.tsx` (corner-lit club wash + crest bled as wallpaper + 36pt `heroTitle`) replaces the deleted `club-header.tsx`, and ⚠ the NATIVE HEADER GOES — the hero draws its own back pill, which can be LABELLED with the competition where the native button could only print the route group `(tabs)`. Three new trays: a POS/PTS/GD/LAST-5 standing strip (drawn only when `bandsApply` holds a row — absent renders nothing, never dashes), a NEXT UP card over the OPPONENT's colour (joined BY NAME — `FixtureView` has no opponent slug — at ⚠ half the mock's alpha, because `clubTint` drops 0068's lightness clamp and Valencia's orange washed the whole card), and an alerts+calendar tray whose alert row INVERTS (solid lime = not subscribed, the invitation; wash = subscribed). The spine's rail becomes a club-tint gradient and its node fill goes translucent (a fixed ground hex stopped matching the mesh); the squad gets one glass card per band | Accepted — verified on the simulator (tinted + no-tint clubs); ⚠ spine, squad and the not-subscribed alert row are BELOW the fold and unseen |
| [0092](./0092-news-uniform-story-cards.md) | News becomes UNIFORM glass story cards (64pt thumb, 3-line headline), reversing 0070's front page and 0071's full-bleed Today lead — 0070's problem was thirty competing cards on a charcoal ground, and on the mesh a glass card is quiet enough to repeat, with the crown and day headers carrying the hierarchy instead. ⚠ The screen maps the WHOLE group: `frontPagePick`'s lead/tiles split had to be removed from `news.tsx` in the same change, or every lead and tile story would have silently vanished. Filter chips go NEUTRAL when unselected (new `ChipButton tone`) — a rail of lime rings read as every league being on at once; the follow pill keeps the accent ring. The Today card leads with a 96pt thumbnail row and stays a three-item doorway; News drops its native header for a lime back link that names its destination | News-screen half superseded by [0129](./0129-news-reel.md) — the reel; the Today-card half and `NewsRow` stand |
| [0093](./0093-sheets-on-their-own-ground.md) | Sheets get their own opaque ground `sheetGround #101316` (the pack's reason: glass over a scrim reads muddy), set once on the root stack's shared options where 0030 put them; groups inside a sheet are flat `glassFill` blends and a pinned bar matches the ground exactly. The platform `Switch` is REAFFIRMED against the mock's hand-built one (0081). ⚠ Colour-only — no layout touched, because inside a `formSheet` `flex: 1` collapses and a pinned header must be `stickyHeaderIndices` (trap 19). The tab bar takes an opaque `tabBar #0a0b0c`: `NativeTabs` draws the system material behind a colour, so the mock's alpha is not ours to set | Accepted — verified on the simulator |
| [0094](./0094-crown-runs-to-the-top.md) | The crown runs to y = 0 — 0087's crown-below-status-bar call is REVERSED — and its gradient becomes a FIXED-HEIGHT layer (`topInset + CrownRamp` 432, the mock's own Clubs number) anchored at the top instead of fractions of the crown's box, which had compressed short crowns' ramps and put Today's title on mid-teal while Clubs' sat on lime. A shorter crown lets the fade run on behind the body (no clip — a clip is a hard edge at the hand-off). The STATUS BAR flips with the scroll: dark glyphs while the bright band is behind it, light past an arithmetic threshold, gated on `useIsFocused()` so a blurred tab cannot hold the bar dark over a pushed screen | Accepted — verified on the simulator |
| [0095](./0095-next-up-joins-the-crown.md) | The Today crown's payload is the screen's LEAD CARD whichever it is — `LivePlate` in play, NEXT UP otherwise — so the head of the screen is one object in both states and 0094's gradient covers the card. `match-board.tsx` is DELETED, split into `next-up-card.tsx` (0052's kickoff contract verbatim: countdown fires the finished/recent/live refetches, never `upcoming`) and `last-result-card.tsx` (first in the body, suppressed while live — the early-return's behaviour by a different mechanism). ⚠ The idle→kicked-off→live crown sequence is unwatched on real data; the payload now swaps COMPONENT at the whistle | Accepted — idle verified on the simulator; live states via gallery previews |
| [0096](./0096-next-up-goes-liquid-glass.md) | The NEXT UP card takes 0090's LIQUID GLASS at card size (GlassView gated, `glassFill` floor, shared sheen + lit-edge overlays) — it was the crown's one opaque slab. The club pair drops to a WHISPER (`ClubWash2.edgeOnGlass` .10/.02): translucent tint over a translucent shell double-blends, so the full alphas and the `#15171a` base they required are gone. The lime ring and accent `VersusBadge` return on BOTH variants (0068's white-on-wash chrome deferred to a wash that no longer exists); faint inks step up to `textSecondary` inside the card — two of the lines that vanished on frosted glass are honesty lines | Accepted — washed variant verified on the simulator; no-wash + TBD via gallery |
| [0097](./0097-club-hero-following-pill.md) | The subscribed club page's hero draws a FOLLOWING pill opposite the back pill (check + `accentWash`/`accentRing` — the settled chrome, never solid lime), because the app's ONE unfollow sat below the fold behind a row labelled "Alerts on" and nothing above the fold even said the club was followed. Its tap opens the SAME alerts sheet as the alerts row — a second door to the one confirm, never a direct unfollow (0082's calendar-entries reason stands) — and it is hidden while not subscribed: the solid-lime alerts row stays the screen's only follow invitation and only lime hero | Accepted — verified on the simulator via deep link; the tap hop is the alerts row's shipped push |
| [0098](./0098-hero-crest-dissolves-not-clips.md) | The hero's wallpaper crest DISSOLVES to zero just above the hero's clip line instead of being chopped by it — the mock hard-clips the same way, but the hero's wash fades to nothing so the cut had no visible container and read as a rendering fault (the NEXT UP card's crest clip stays: a visible card edge explains it). Built as `atoms/fade-out-image.tsx`, horizontal BANDS over one cached expo-image source stepping a half-cosine (≤1.5%/band, imperceptible at watermark alphas) — NOT an rn-svg `<Mask>`, because Bundesliga and PL crests are `.svg`-only on the wire (probed 2026-09-01) and rn-svg's raster-only `<Image>` would blank them silently; NOT masked-view, a native module the dev build can't load. Fade endpoint derived from the bleed offset so fade and clip cannot drift | Accepted — verified on the simulator (raster + svg crests) |
| [0099](./0099-onboarding-joins-the-crown-shell.md) | Onboarding joins the crown + aurora shell, REVERSING the repaint's floodlight carve-out on Ed's call: all three screens sit on the mesh, the welcome takes a TITLE-LESS crown (the gradient head alone — `CrownProps.title` is optional now, the brand moment below carries the words), the picker and primer take the full crown with `StepDots` as head `meta` and search + pills riding the payload. The picker's tiles become the rail's own LIQUID-GLASS `ClubBubble`s in a thirds grid — the molecule grew `checked?: boolean` (checkbox semantics, check only when picked; omitted keeps the rail's one-action button, 0082's rule un-weakened). `BRIGHT_BAND` exported for the picker's inline status-bar flip (it needs a pinned footer, so no `ScreenScaffold`); the primer's group goes glass; `Glow`, `crestPick`, `pickBadge` swept | Accepted — three screens verified on the simulator; picked-state + taps need a device pass |
| [0100](./0100-table-caption-under-the-title.md) | The Table crown's caption moves UNDER the title as the crown `subtitle` (the Clubs slot), amending 0087's right-shoulder block: the mock's construction only fits the English "Table" — beside the shoulder, "Clasificación" got ~130pt and wrapped MID-WORD. Sentence-case caption replaces the uppercase eyebrow pair; trap 2's null-caption degradation unchanged; `meta` stays for captions beside titles that are short in EVERY language | Accepted — verified on the simulator (ES) |
| [0101](./0101-signed-out-avatar-attention-ring.md) | The signed-out account avatar gets a SPINNING attention ring (a gradient comet arc orbiting per `Motion.orbit`, soft same-ink shadow as the glow — DARK `onCrown` ink on the crown, where lime is invisible on its own band), a drawn person-silhouette glyph replacing the mark (an account door says "you", not "app" — new `atoms/person-glyph.tsx`, both avatar sizes), and `Size.avatar` 36→42. Two recorded stances narrowed, scoped to the `attention` prop alone: 0015's no-glow gains a fourth exception, and `skeleton.tsx` stops being the only loop. ⚠ `attention` only while signed OUT — the spin is the sign-in invitation, never the settled state | Accepted — verified on the simulator (two frames show the arc rotating) |
| [0102](./0102-widget-club-filter-matches-involvement.md) | The Edit Widget club filter matches INVOLVEMENT, not ownership: entries gain `homeSlug`/`awaySlug` (snapshot v4, optional in Swift — a v3 file degrades to owner-only) and both `rows(...)` filters use `Entry.involves(club)`. Fixes the shipped blank: a derby between two followed clubs is ONE deduped entry whose `clubSlug` is the home side (0029's tie-break), so a widget configured to the away side matched nothing — "Sin partidos programados" with the club's fixture in the snapshot. ⚠ Accent + deep link still follow `clubSlug` on a filtered derby (recorded, not fixed); a club-filtered widget still shows ONE row by contract | Accepted — verified in the node + swiftc harness on live data |
| [0103](./0103-email-password-sign-in.md) | Email/password sign-in ships as the sign-in sheet's quiet third option (4.8's Apple ≥ Google order untouched), reversing 0038's "no email/password" clause: a full-detent form sheet (Entrar / Crear cuenta, forgot-password, 60s resend lock) calling Supabase directly like the other providers, with EVERY mail link landing on the web's own locale-prefixed pages — no native deep links, no in-app reset screen, no Supabase config or template changes. `signUp` with no session is SUCCESS; `email_not_confirmed` branches to a neutral notice + resend; wrong password and unknown email are ONE message and reset requests answer uniformly (enumeration resistance); errors map from GoTrue `code`, never prose. New: `form-field` + `segmented-control` molecules, `google-mark` atom (the one literal-hex exception to the theme rule). ⚠ No consent checkbox — Mailchimp is web-server-only; the recorded parity gap | Accepted |
| [0104](./0104-widgets-adopt-the-app-shell.md) | The widgets adopt the app shell, AMENDING [0087](./0087-crown-aurora-shell-adopted.md) §10's `targets/` carve-out: `Tok.ground` moves `#0a0b0c → #0f1316` and a new `MeshPlate` draws the aurora pools as the container background of all three tiles (YOUR WEEK's floodlit `Plate` deleted). ⚠⚠ **`SwiftUI.glassEffect()` DOES NOT WORK IN A WIDGET — it erases its own content, silently.** Built first with real glass on iOS 26 over a painted fallback; on the 26.5 simulator the whole modified subtree rendered as NOTHING (the rail column and the live ledger vanished, text and all, while the eyebrow and footnote around them drew). Isolated to a bare `content.glassEffect(.clear, in:)` — compiles, typechecks at every floor, clean build, no log line. So `GlassSurface` is ONE painted path at every version, which is what most of the app draws anyway; `GlassEffectContainer` was written and deleted. ⚠⚠ Copying theme.ts's `Mesh` verbatim also rendered DEAD — it centres two pools OFF the surface (`cx` −0.12/1.08), which a 930pt screen still catches the cores of and a 158pt tile does not, giving a grey-green murk with a dead centre exactly where the type sits. New `theme.ts` `MeshTile` pulls the centres onto the tile's edges and widens each fade — ⚠ the three ALPHAS are unchanged, they were never what was wrong — calibrated against a sampled screenshot of the real Today ground (p90 green 38 → 45, the top of the app's own range). Glass lands only on what is genuinely a panel (rail column, `recess`-scrimmed live ledger, NEWS story rows), never as an inset card on the mesh (0086 §3). ⚠ `.contentMarginsDisabled()` is configuration-wide with no per-family form, so NEXT hands its three Lock Screen families their `widgetContentMargins` back by hand. Crest fallback tiles go unfilled (`tone: .open`) — ⚠ `.solid` stays the DEFAULT because `_shared/` compiles into the notification extensions. New `Tok.scrim` pins the news lead's fade at `#0a0b0c` so it does not follow the ground lighter. ⚠⚠ The Live Activity changes by ADDITION only (the coolest mesh pool at 0.28 + the lit top edge, zero height against the ~160pt cap) and takes NO glass: it does not own its rectangle. No wire change, no snapshot bump, no `Cadence` value touched | Accepted — verified on the iOS 26.5 simulator across five sample states (`week1`/`week3`/`week3es`/`live`/`ft`), ES worst case included; ⚠⚠ the NEWS tile has NEVER been rendered (it is not placed and a widget cannot be placed without a tap), and the Live Activity cannot be — nothing here starts one |
| [0105](./0105-lpr-clausura-and-league-capability-flags.md) | Puerto Rico ships as ONE more `LEAGUES` entry (`lpr-pro-clausura`, `LPR Clausura`, `clubCount: 11`, `zones: []`) and the catalogue's dormant capability flags are finally READ — the first league whose source cannot feed the whole app. `rounds` gets `ROUND_LEAGUES`, taken by the Matchdays screen and `useAllSeasonJornadas`, so PR is unselectable there and never asked for a matchweek index (its jornada route answers `200` with `matchweeks: []`, NOT a 404 — ungated it draws a 20-round pager where every round is empty); new `matchEvents: false` REMOVES the events disclosure rather than letting it open on "not published yet" copy, because the federation enters no player events ever; new `calendarYearSeason: true` feeds `leagueSeasonLabel` so a Ponce player reads `2026`, not `2026/27`, via a new `leagueOfClub` that is deliberately NOT gated on `bandsApply`. ⚠ There is NO capabilities field on the wire and none is planned — the table is copied by hand from `CRONOGOL-API.md`, never inferred from payload shape (an empty array is what a WORKING league serves mid-sweep; traps 1 and 32). ⚠ PR shipped with NO artwork and the backend added an SVG league crest hours later — NO client change was needed (both artwork resolvers already fell through to `primary`, and `LeagueSwitch` already routes SVG sources to `expo-image` at .45 because 0089's grayscale `FilterImage` is bitmap-only and draws a vector as an EMPTY chip; PR is the second league on that path after Serie A), so 0031's text branch is once again the unexercised fallback — the display name still serves VoiceOver, the `FINISHED TODAY` header (which now prefers OUR name over the wire's `LALIGA EA SPORTS`-style copy) and artwork failure. ⚠ PR's completed season also exposed a SHIPPED layout bug — five form chips are a fixed 142pt against a `flex: 1.45` share worth 115, so the fifth ran outside the tray, invisible until now because no league had ever served five results (trap 56). Names arrive `"SURNAME SURNAME, GIVEN"` — `playerDisplayName` flips on the FIRST comma only and `playerFamilyName` gives the token its surname half; ⚠ neither title-cases (`O'NEILL`, `DE JESUS`), and Real Madrid's 24 names verify unchanged. `live` stays unread (0032's note stands — PR HAS clubs); `fixture-list.tsx` deliberately ungated (unreachable from a non-rounds league); no `LeagueBand` entry; Apertura deferred until the backend registers it | Accepted — verified against production data and on the simulator |
| [0106](./0106-next-up-card-refuses-a-stale-pick.md) | The club page's NEXT UP card refuses a pick whose kickoff is more than 48h past, WITHOUT reversing `nextUpIndex`'s deliberate no-clock rule: WHICH row is next is still the order-based pick and the spine still lists every fixture in place — what the card and the spine's ACCENT (lime date, filled node, lit border) both decline is CALLING a February match "next". ⚠ The spine half was missed in review and caught on the simulator: an accented February row one scroll under an absent card is the same claim in another visual language. ⚠ `now` is read ONCE at mount (`useState(() => Date.now())`, `countdown.tsx`'s pattern) — `Date.now()` in a render body is impure and lint rejects it. Forced by Puerto Rico, where the federation publishes a COMPLETED table (110/110 played, form on every row) over fixtures that all stay `scheduled` with null scores, permanently. ⚠ 48h not 2h because a past kickoff at `scheduled` is NORMAL for every league inside the 3-hourly sweep window, and a tighter guard would blank a correct card on a LaLiga Sunday. A null or unparseable kickoff is never stale — it has claimed nothing, and the card's TBD state is right. Verified on live data: Ponce's `2026-02-09` pick suppressed, Real Madrid's `2026-09-04` still drawn. ⚠ Anything new reaching for "the next match" must choose: the list's next ROW (`nextUpIndex`) or a CLAIM about the future (`nextUp`) | Accepted — verified against production fixtures for both a Puerto Rican and a LaLiga club |
| [0107](./0107-next-tile-names-its-sides.md) | The small NEXT tile's upcoming state names its sides — each a centred COLUMN, crest (32pt) over name (10.5pt, followed side in the accent, 0086's marking), the in-app card's stacked idiom — and takes the hairline-then-countdown rule row from `next-up-card.tsx`; countdown 20→22pt, the venue LEAVES the footer (`kickoffLabel` alone — it only ever rendered truncated at this width), `CrestPair` deleted with its one call site. Every size is a harness-measured number against the Display-Zoomed SE's 115pt content box: the column's minimum is 114.5pt, the timer's un-scalable `11:59:59` is 102pt at 22pt (111 at 24, which is why not 24), `R. Sociedad` lands at scale 0.85 in an SE half-column. Live ledger, accessories, provider and reload budget untouched — paint only, no wire change, no snapshot bump | Accepted — verified on the iOS 26.5 simulator: `week3` worst-case sample (EN) and real Betis/Madrid data (ES) |
| [0108](./0108-widgets-speak-the-day.md) | The widgets SPEAK the day (Ed picked direction B off the mockup artifact): medium hero says `Viernes` at 15pt accent on its own line (the inline `VIE` tag read as furniture — lime + tracking is the tile's TAG voice), the rail says `Dom 6` (a code beside a number reads as a date; costs no width — the time column is wider), the small footer says `Sábado 21:00`. Snapshot v5 adds `kickoffDayName` + `kickoffDayDate` (pre-formatted, sentence-cased, `Intl` long weekdays via `phrases.intl`; both optional in Swift, v4 degrades to the old forms). ⚠ The spoken hero is 114pt against a 124pt standard body / 112 mini / ≤109 SE, so `heroColumn(_:spoken:)` branches on measured body height (≥118) and compact tiles keep the inline tag — which is why `kickoffDay` still travels. ⚠ "Which Friday" stays unanswered on the hero — that is direction A's dated line and a new ADR if wanted | Accepted — verified on the simulator (ES real data 12h + EN `week3` sample 24h) |
| [0109](./0109-rail-solo-card.md) | The medium rail becomes a CARD when it holds one match (the two-club reader's everyday state): clubs named in full anchored top (20pt crests, 10.5pt names, followed in accent), `Dom 6` + 18pt time anchored bottom, flexible gap between — replacing a squeezed multi-row row that truncated `Valen…`/`Barce…` beside a 12-hour time and floated in ~90pt of empty glass. Clubs-first is 0107's order, NOT the hero's — the hero beside it already leads with the kickoff. `roomy` reuses 0108's 118pt height branch (full card ~102pt min v a zoomed SE's ~95); compact tiles take a smaller cut. No wire change; v4 degrades via `railDay`'s uppercase fallback | Superseded by [0127](./0127-medium-tile-day-spine.md) — the rail is gone; one fixture is the day spine's opened row filling the tile |
| [0110](./0110-deep-links-anchor-on-the-tab-shell.md) | Deep links anchor on the tab shell: the root layout exports `unstable_settings = { anchor: '(tabs)' }`, so a COLD launch from a widget tap (`altagamafc://club/{slug}` — YOUR WEEK's `Link`s, NEXT's `widgetURL`, the Live Activity) builds `[(tabs), club/[slug]]` instead of stranding the reader on a club page with no tab bar, a no-op back pill and nowhere to swipe back to. The notification path never had this bug — `routing.ts` lands on `(tabs)` and pushes; the raw-URL path is resolved by expo-router itself and this is its one knob. Warm links and normal launches unchanged. ⚠ JS-only but no EAS Update — reaches a phone with the next build | Accepted — three cold `simctl openurl` launches + a live dev-session tap all logged `stack=(tabs),club/[slug]/index`; device pass on the next build |
| [0111](./0111-live-activity-crests-are-a-fourth-keep-list.md) | The Live Activity's crests become a FOURTH keep-list on the App Group sweep — followed clubs' fixtures inside 48h, capped at 8, both pinned and warmed — because it is the ONE crest consumer whose demand set is chosen by the SERVER while the app is closed (push-to-start at T−10; `CrestView` reads the container and never the network). Neither existing selection covers it: the reminder set (soonest 12) exists only while `alertReminder` is on, and ⚠⚠ a reader with reminders OFF still gets cards, because 0055 §5 hangs activity consent on `alertGoals` — leaving the widget's ≤ 6 as their only writer, chosen for what the WIDGET draws and routinely missing tonight's match once in-play fixtures eat its budget. ⚠⚠ `kickoffTbd` and `postponed` fixtures are KEPT — the one place this selection disagrees with both others, which drop them, leaving such a fixture in no write set and no keep-list while the backend still push-to-starts it on reinstatement (`cancelled` is dropped). Three delete paths closed with it: ⭐ the prune now REFUSES to sweep the App Group while either pin set is `null` (0047 initialised the widget pin to `[]`, and the 7-day `useUpcoming` normally lands before the 21-day `useWidgetWindow`, so the sweep routinely ran with no pins and cut the container down to the reminder queue — a `widgetWindow` that merely FAILED did it all session); ⭐ an unsynchronised write-vs-delete race where `applyReminders`' fire-and-forget prune deletes the directory `warmLongLookCrests` is mid-download into, the throw swallowed, the crest silently absent **for a fixture that was in the keep-list** — closed with an in-flight set the sweep treats as wanted; and the empty-keep-set case. ⚠ Amends 0085 §1's claim that the card's crest is "already warmed by `crests.ts`" — it was not, for the fixtures that matter most. ⚠ A fifth selection must join the union or be deleted silently (trap 17). ⚠⚠ Does NOT close the app-never-opened case — a silent pre-kickoff warm push needs `expo-task-manager` and reverses the no-`content-available` non-choice in 0080/0061, and gets its own entry. `_debug/widgets.tsx` now lists EVERY crest directory, not just snapshot entries — the card's fixture was invisible in the old list whether its artwork was there or not | Accepted — pure selection verified in a plain-node harness (8 cases); ⚠ not yet on a device, since nothing in this repo can start an activity |
| [0112](./0112-meta-ads-read-over-mcp.md) | Meta Ads state is READ through the claude.ai Meta MCP connector, never through pasted Ads Manager screenshots — and ⛔ **written by nobody**: every mutating tool (`ads_create_*`, `ads_update_entity`, `ads_activate_entity`, `ads_boost_ig_post`) is present, unexercised and therefore unverified, and the asymmetry forces the rule — a failed read costs nothing while a successful write spends money publicly under a brand mark and keeps spending after the session ends, with no dry-run and no sandbox on this account. Two findings behind it. ⚠⚠ `ads_get_ig_accounts` returns `[]` **while the ads it would describe are serving** (real `effective_instagram_media_id`s, ~22k impressions each) — an empty array means the connector cannot ENUMERATE the asset, NOT that Instagram is unlinked, and the obvious "go re-link it" reading would risk the one thing that currently works; cause left open between "no owning business on the ad account" and "`instagram_basic` never granted", which are indistinguishable from here. ⚠⚠ Alta Gama creatives are split across TWO Pages — `altagama.fc` (`1208965545641551`) carrying ALL current spend, `Alta Gama FC` (`1234014533133945`) carrying the NEWEST creative — so the Page holding the money and the spaced mark [0042](./0042-brand-spelling-spaced-form-reinstated.md) leads on are different strings; ⛔ which is canonical is Ed's call and is left OPEN, so **no creative may be published on either Page** until he makes it, because publishing casts the vote. ⚠ `ACTIVE` ≠ spending here (12 active, 2 delivering) and Social-Logistics is a SHARED account carrying six unrelated brands back to 2022 — every query filters or it reports on `hodl_it_crypto`. No ads code enters this repo ([0009](./0009-backend-is-only-data-gateway.md)); the eventual dashboard is a `senpai-backend` ops-console screen (its `0052`). Detail: [.claude/META-ADS.md](../META-ADS.md) | Accepted — read path verified 2026-09-04 across five tools; ⛔ write path deliberately unexercised |
| [0113](./0113-same-day-next-up-deck.md) | NEXT UP becomes a same-day DECK in the crown (Ed's ask, HTML mock approved first): `nextUpDeck` = `dayGroups(upcomingMine, zone)[0].items` — every followed-club fixture on the SOONEST kickoff's reader-zone day, `deck[0]` still the single card's fixture by construction, TBD floating days never merging with confirmed (an all-TBD deck is the only mixed case; 9-assertion harness). New `organisms/next-up-deck.tsx` stacks FULL `NextUpCard`s — ⚠⚠ every card stays MOUNTED, hidden layers at opacity 0, never strips: a card's own `Countdown` is the only observer of its kickoff (0052/0078), and every card carries the screen's one `onKickoff` unchanged. ⚠⚠ Deck cards are OPAQUE (`NextUpCard surface="opaque"`, AMENDING 0096 — glass is the SINGLE card's surface now): glass cannot stack — a card behind it ghosts its text through the blur, a featureless shell glows, slivers read as a pasted slab (trap 59, three iterations; Ed called the surface change). On the opaque lead the waiting cards are simply VISIBLE, dimmed by scrims (0.42/0.69) that fade off the riser as the drag reveals it — the mock's exact look — the club wash returns to the full 0087 alphas (the whisper was glass's double-blend problem), and the ground is `DeckGround`, the crown BAKED as a vertical ramp calibrated against the glass card's rendered colours (`card`'s charcoal was "waay too dark" on the bright band; the venue takes `washInk`, 0068's ink returning with the full wash); no shadow anywhere (murk through glass) and the commit exit overshoots to 1.25× width so the spring's settle tail stays off-screen; max 3 drawn; `StepDots variant="dot"` counts them all. Swipe either way wraps; pan `activeOffsetX ±10` / `failOffsetY ±8` — the app's FIRST scroll-contended gesture, vertical drags still scroll the crown away. Depth runs on a `topSV` shared value so a commit swaps every layer in one UI batch; reset is the screen's `key` remount (a shuffle is a peek, not a preference — no effect writes state). The app's FIRST spring (`Deck.spring`, on `Deck` because `Motion` is durations-only) + `hapticShuffle()` light impact on commit only + round dots — all three Ed's explicit picks. Answers 0070's carousel objection: the top card is always the soonest (nothing needed hides; peeks+dots disclose the rest) and "no snapping carousel" expired with 0065. One VoiceOver stop; increment/decrement actions shuffle without the gesture. ⚠ Tilting a `GlassView` unproven on device; gesture pass is BY HAND (the simulator cannot script swipes) | Accepted — selector harness-proven (9), tsc + lint baseline clean, all four gallery decks + the single-card regression verified on the simulator; ⚠ gesture/device pass pending |
| [0114](./0114-widgets-under-system-glass.md) | The widgets become a DESIGNED surface under the system's glass (iOS 18 Tinted / iOS 26 Clear home screens), AMENDING [0104](./0104-widgets-adopt-the-app-shell.md) §8: `GlassSurface` now paints fill + 0.5pt hairline in `.accented` instead of bailing (the panels must stay delineated — the rail's "glass edge is the seam" layout leans on it), while `.vibrant` still gets bare content and `MeshPlate` still hands every non-fullColor mode `Color.clear`. The key mechanic: accented rendering preserves OPACITY but flattens color to WHITE — the white-alpha `Tok` system survives by construction, the lime flattens, and anything BLACK inverts (trap 60): the `recess` scrim would LIGHTEN the panel it sinks, so accented panels take no scrim, no ring, no lit edge (the system slab lights itself). The lime voice is marked `.widgetAccentable()` at CALL SITES only (never inside `Mark`/`W.eyebrow` — the accessories chose their own set): header eyebrow + Mark on all three tiles, LiveEyebrow, the TU SEMANA capsule as one group, spoken day, topic eyebrow, EmptyState's Mark, and every followed-club name via `.widgetAccentable(followed)` so "which side is yours" survives the mode that erases color. ⚠⚠ The NEWS lead draws its PHOTO only in `.fullColor`: its near-black `Tok.scrim` fade tints to a white .62→.94 wash that erases the picture and drowns the headline on it, the gradient cannot opt out (`widgetAccentedRenderingMode` is `Image`-only), so a tinted render takes the imageless lime-ruled branch. `CrestView` and `_shared/` behavior untouched; stale glassEffect prose corrected; no API to force glass in the default appearance exists (`.widgetTexture(.glass)` is visionOS-only) and trap 52 stands unre-tested | Accepted — typechecked at every floor; ⚠ visual pass pending: the Customize → Tinted/Clear toggle is BY HAND on the simulator, screenshots scriptable after it |
| [0115](./0115-widget-tiles-take-a-lifted-ground.md) | The widget tiles take a LIFTED ground — `Tok.groundLift` (`white.opacity(0.09)`, DEVICE-calibrated — simulator rounds settled 0.045, the phone's OLED read it still too dark and doubled it; ⚠ judge this token on hardware) painted over `Tok.ground` UNDER the mesh pools, widgets only — amending [0104](./0104-widgets-adopt-the-app-shell.md)'s exact match to the app's `#0f1316`: the app's ground fills the display but a tile floats on a wallpaper usually darker than it, where the shared hex read near-black (Ed: "a bit lighter", after [0114](./0114-widgets-under-system-glass.md) measured translucency impossible — alpha composites over black, so paint is the only lever). A lift, not a second hex, so removing the overlay makes the tile the app again; `Tok.ground` and the calibrated `MeshTile` alphas untouched; fullColor branch only. ⚠ 0.06 is a taste value tuned as ONE number — never compensate via `ground` or the pool alphas | Superseded by 0126 — the plate is plain black, the lift deleted |
| [0116](./0116-crown-league-chips-larger-cut.md) | The CROWN's league chips take a larger cut — `tone="crown"` (Matchdays, Table) draws 52pt chips with a 66×33 mark box (1.5× 0089's mark) via new `Size.leagueChipHCrown`/`leagueChipMarkWCrown`/`leagueChipMarkHCrown`, amending [0089](./0089-league-chips.md)'s one-size geometry; `tone="ground"` (Clubs) keeps 36 / 44×22. Scoped by TONE, not a new prop — the crown row is those screens' primary control, now clearing `minTouch` which 0089's 36 never reached. ⚠ Device-judged (first cut 44 / 54×27 read too small on the phone — 0115's lesson) | Accepted — Ed's call off a device screenshot |
| [0117](./0117-league-rail-liquid-plate-slide.md) | The league selector becomes a SOLID RAIL with a gliding liquid-glass plate — `LeagueSwitch` trades its per-chip ink wells for one `tabBar`-ink capsule (`Radius.pill`, inset by new `Size.leagueRailPad` 6, both tones one code path) under a `GlassView glassEffectStyle="clear"` plate that springs to the tapped chip on new `Glide.spring` (26/340/1 — the app's second spring, beside `Deck` because `Motion` is durations-only), degrading to a flat `segThumb`+`accentRing` lozenge without liquid glass (0090/0096 pattern). Measurement is per-chip `onLayout`; first placement is ASSIGNED (segmented's `placed` rule); reduced motion assigns everything. 0089's double inversion becomes a `Motion.quick` crossfade of stacked grayscale `FilterImage` / colour `Image` (SVG keeps the opacity ramp); `hapticToggle()` on change-only press, at the tap not the glide. Ed's explicit non-picks: press-scale, scroll-into-view, `LeaguePills`. ⚠ `leagueRailPad` device-judged — the rail eats crown height 0116 calibrated without it | Accepted — tsc + lint clean; simulator pass pending |
| [0118](./0118-league-rail-static-and-plate-drags.md) | The league rail goes STATIC and its plate DRAGS like the nav's — the ScrollView is gone (every league an equal flexed slot across the gutter, slot `gap` dropped so the plate spans one cleanly) and a `Gesture.Pan` with new `Glide.activateX` 10 / `failY` 8 (the deck's contention thresholds) tracks the plate under the finger, snapping `Glide.spring` to the nearest slot on release with the same change-only `hapticToggle()` commit. Amends [0117](./0117-league-rail-liquid-plate-slide.md) (no scroll) and [0116](./0116-crown-league-chips-larger-cut.md): the crown MARK returns to the first cut 54×27 so five slots fit (~61pt each); chip heights stand. Finger tracking stays under Reduce Motion (0113's direct-manipulation rule), the snap assigns; plate shared values move up to the parent (gesture writes, `SelectionPlate` reads — DeckLayer split inverted); marks do NOT light under the hovering plate (recorded divergence from the system bar). ⚠ 54×27 is the cut 0116 rejected — re-judge on device; the revisit lever is chip height, not scrolling | Accepted — tsc + lint clean; simulator pass pending |
| [0119](./0119-league-plate-takes-the-navs-material.md) | The league plate takes the NAV's material — `GlassView` flips `clear` → `regular` + `isInteractive`, amending [0117](./0117-league-rail-liquid-plate-slide.md): "clear" is the convention for glass SURFACES (0090/0096) but this plate impersonates a system CONTROL, and beside the real NativeTabs bar it read flatter than the thing it echoes (Ed, on the simulator). Wrapper keeps `pointerEvents="none"`; the pre-26 flat fallback stands. Rule of thumb recorded: surfaces "clear", chrome impersonators "regular" | Accepted — Ed's call comparing rail to bar on the simulator |
| [0120](./0120-league-rail-is-glass-untinted.md) | The league rail is GLASS, and UNTINTED — the rail's ground becomes `GlassView regular colorScheme="dark"` with no tint (flat `tabBar` paint demoted to the pre-26 `railFlat` fallback), and the plate pins the same dark scheme, amending [0117](./0117-league-rail-liquid-plate-slide.md)/[0118](./0118-league-rail-static-and-plate-drags.md). Two screenshot-judged findings against the real bar: flat paint reads MATTE beside the system material, and an opaque `tabBar` `tintColor` crushes the glass to solid black — the bar's darkness is just the untinted dark-appearance material over dark content. On the bright crown the rail reads green-glass, lighter than the bar over the body — the material adapting, authentic nav behaviour; the darkening dial if wanted is a PARTIAL-alpha tint, device-judged. Free wins recorded: Bundesliga's wordmark legible again, jornada chips refracting through the edge like the bar's own list. Glass-on-glass needed no `GlassContainer` | Accepted — screenshot-judged beside the bar; device pass pending |
| [0121](./0121-league-plate-is-the-lens.md) | The league plate is the LENS — `glassEffectStyle="clear"` + `isInteractive`, untinted, scheme-free: the transparent magnifying bubble the nav shows under the finger (Ed's close-up screenshot), amending [0119](./0119-league-plate-takes-the-navs-material.md). The walk: 0117's clear-without-interactive is a flat wash, 0119's regular is frosted smoke, and clear WITH interactive is the lens — **the interactive flag is what turns "clear" into the lens**. Rail keeps [0120](./0120-league-rail-is-glass-untinted.md)'s untinted regular dark glass; flat pre-26 fallback stands. Glass grammar now: surfaces clear non-interactive, chrome impersonators regular, the one finger-following element the lens. Lens qualities show in MOTION — statics confirm the material, the feel is a device check | Accepted — screenshot-judged; drag feel pending on device |
| [0122](./0122-league-plate-two-states-lens-under-finger.md) | The plate wears TWO STATES — frosted at rest, the LENS under the finger — amending [0121](./0121-league-plate-is-the-lens.md): two stacked plates on the same shared values, [0119](./0119-league-plate-takes-the-navs-material.md)'s frosted regular BEHIND the marks at rest (mark stays crisp) and the clear+interactive lens OVER them, invisible until touched. `lensUp` swaps them DISCRETELY (0 ↔ 1 — a crossfade KILLS the glass: fractional ancestor alpha disables a `UIVisualEffectView`, the lens drew nothing and the selection vanished under a held finger, simulator-caught): up on `onTouchesDown` within the plate's span (tap-and-hold, before movement) or pan activation, down on `onFinalize`. The always-on lens smeared the resting mark into red streaks (screenshotted) — refraction is the point under a finger, vandalism at rest. The lens is MOUNTED only while touched and neither plate takes touches (native interactive response abandoned — unreliable under RN compositing); the magnify + light distortion is the lens material refracting the mark it rides over (recorded trap-59 divergence) plus OUR `Glide.swell` spring. THREE glass findings, each simulator-caught: fractional ancestor alpha disables the effect (swap discretely, 0 ↔ 1 only); glass mounted inside an alpha-0 ancestor stays blank when alpha returns (mount on touch, born visible); a transform scale rasterizes the sampled backdrop into a murky smear (swell by BOUNDS growth, recentered — A/B'd via scripted hold). Wrapper lost `overflow: hidden` (pill on the GlassView). FOURTH finding: the public material BLURS close content whatever its flags (interactive on/off identical, A/B'd) — so magnification is COMPOSED: sharp `Glide.magnify` (1.18) mark copies in a clipped, counter-translated viewport inside the lens, a true magnifier window; glass supplies rim + ambience only. No liquid glass ⇒ no lens state, flat fallback is the whole selection | Accepted — hold/tap verified by scripted touch on the simulator |
| [0123](./0123-league-marks-full-colour-at-rest.md) | League marks are FULL COLOUR at rest — one plain `Image` per chip, idle receding by the 0.72 dim alone, superseding [0089](./0089-league-chips-grayscale-inactive.md)'s grayscale-inactive + double inversion (geometry and artwork-only survive): 0089 earned colour on the BRIGHT crown band, but on the dark glass rail (0117–0120) a desaturated mark reads WHITE, not "off" (Ed). Deleted wholesale as desaturation-only servants: the `FilterImage` grayscale layer, the stacked two-layer mark crossfade, and the whole SVG fork (`isSvgUrl`/`SVG_IDLE_OPACITY`) — Serie A's recorded divergence closes for free, and `react-native-svg/filter-image` leaves the repo (trap-40 dormant). Idle hierarchy's one dial is the 0.72 chip dim, device-judged | Accepted — verified by scripted tap on the simulator |
| [0124](./0124-league-rail-hovers-under-the-held-lens.md) | The rail HOVERS under the held lens — it GETS BIGGER in place: the capsule backdrop, split from the layout box onto its own absolute layer, inflates `Glide.hover` (3pt; first cut 4, eased) per edge via animated INSETS on `Glide.spring`, settling on release; same triggers as the lens so plate, swell, and hover move as one gesture response (amends [0122](./0122-league-plate-two-states-lens-under-finger.md)). First cut translated the rail up 6pt and was WRONG — "the main nav stays in position but hovers … the rail is supposed to get bigger" (Ed). Bounds, never a transform (0122's finding 3); chips, plates, magnifier, `onLayout` boxes and gesture coords never move — zero compensation. Reduce Motion: no hover; no shadow (glass + RN shadows misbehave). ⚠ Mid-hold the capsule overlaps neighbours ≤3pt/edge — device-judge `Glide.hover` | Accepted — hover verified by scripted hold on the simulator |
| [0125](./0125-widget-plate-goes-flat.md) | The widget plate goes FLAT — one solid opaque colour (`Tok.ground` + [0115](./0115-widget-tiles-take-a-lifted-ground.md)'s 0.09 lift, under the 1pt `plateTop` rule), the `MeshTile` pools deleted from `MeshPlate`, amending [0104](./0104-widgets-adopt-the-app-shell.md)'s mesh-on-widgets in part (shell adoption, `GlassSurface` and trap 52 stand) — Ed: "no more gradient". Asked with [0114](./0114-widgets-under-system-glass.md)'s measurement spelled out (container alpha composites over BLACK — a transparent tile is impossible in fullColor), Ed chose solid opaque and kept both flat finishes; Tinted/Clear still get `Color.clear`. ⚠ `Tok.mesh`/`Pool` STAY — the Live Activity floodlight samples `mesh[2].color`; the NEWS scrim and `GlassSurface` edge-fade mask are gradients but not the ground, untouched. `MeshPlate` keeps its name (referenced everywhere), its doc records the mesh is gone. A faint neutral top sheen remains in screenshots on EVERY tile — the system's own widget-glass lighting, not our paint (pixel-swept: zero hue variation, horizontally constant) | Accepted — pixel-swept flat on the iOS 26.5 simulator; ground COLOUR superseded by 0126 (black); no-gradient NARROWED by [0128](./0128-widget-tiles-take-a-tray-shell.md) — the tray shell's one-corner glow is approved decoration, solid-opaque stands |
| [0126](./0126-widget-plate-goes-black.md) | The widget plate goes BLACK — `MeshPlate` paints new `Tok.plate` (`Color.black`) under the kept 1pt `plateTop` rule, superseding [0115](./0115-widget-tiles-take-a-lifted-ground.md) wholesale (`Tok.groundLift` deleted — MeshPlate was its only consumer, 0123's dead-servant rule) and amending [0125](./0125-widget-plate-goes-flat.md)'s COLOUR only (no-gradient / solid-opaque / top-edge stand) — Ed, looking at the lifted slate live: "just make the widget background a black color". The tile's relationship to the app's ground ENDS here (0104 matched it, 0115 lifted it, black tracks neither); `Tok.ground` stays UNPAINTED as the app-mirror reference `activityGround`/`scrim` compare against. Tinted/Clear still get `Color.clear` (0114). If black reads wrong on device the lever is `Tok.plate` | Superseded by [0128](./0128-widget-tiles-take-a-tray-shell.md) — the tile is a tray shell (`#0b0d0f` plate in a `#17191b` tray, corner glow); black lasted a day |
| [0126](./0126-concurrent-live-matches-stack-as-a-deck.md) | Concurrent live matches STACK as a deck (Ed: "just like the up-coming cards") — the live tiers MERGE instead of cascading to one winner (amending [0066](./0066-live-card-reads-the-live-route-directly.md)/[0078](./0078-kicked-off-lead-card.md)): new `boardLives()` = routeLive ∪ kickedOff ∪ boardLive, all pluralized, deduped by fixture id with the higher tier winning — ⚠⚠ the route-vs-kickoff dedupe is load-bearing (`seenLive` writes in an effect AFTER render, so a fixture's first live row satisfies both tiers on one render) — because the cascade recreated 0078's vanishing-match bug one level up: match A live on the route hid match B thirty seconds past its whistle, and a followed Premier-League club's sweep card was invisible while ANY LaLiga match was live. Order is GLOBAL earliest kickoff (Ed's pick over freshest-tier-first; ties keep tier order by stable sort) — a stale sweep card that started first may LEAD a fresher route card, one swipe away. The 0113 deck mechanics are EXTRACTED verbatim into generic `organisms/card-deck.tsx` (`NextUpDeck` now a thin wrapper, call sites byte-stable; new `organisms/live-deck.tsx` the live one) with two no-op-for-NextUp additions: waiting layers take NO touches (a peeking disclosure strip must not toggle a hidden panel) and hide from VoiceOver, and `onShuffle` fires on commit — plus one generalization the live cards forced (simulator-caught): ⚠ waiting cards show through a lead-sized, bottom-aligned clipped WINDOW and the container is sized by the LEAD, because live plates are NOT equal-height (sweep carries FeedAge + a longer note; kicked-off has no disclosure) and 0113's math left a taller waiting card's content hanging below the lead — equal heights collapse to 0113's exact terms, so NEXT UP is pixel-identical. `LivePlate surface="opaque"` for deck layers is `DeckGround` + the plate's own `plateDark` composited — the glass plate's pixels produced once, ZERO new tokens, no calibration round. One events panel, on the LEAD only (Ed's pick): controlled `eventsOpen`/`onToggleEvents` (solo path keeps its own state), collapsed on shuffle COMMIT never mid-drag, VoiceOver reaches it via a new `activate` action and the stop un-merges while open. Screen renders `boards`: 2+ → `LiveDeck` (membership key, NO zone term — a kickoff→route upgrade keeps its id and upgrades in place), 1 → solo plate unchanged, 0 → NEXT UP; LAST RESULT gates on length 0 | Accepted — 19-assertion selector harness incl. old-cascade equivalence, tsc + lint + export clean, all four gallery decks + both regressions simulator-verified, shuffle by scripted drag; ⚠ haptic/VoiceOver/Reduce-Motion and a real multi-live matchday BY HAND |
| [0127](./0127-medium-tile-day-spine.md) | The medium tile becomes a DAY SPINE (`handoff_widget-redo/`): one timeline in a fixed 48pt date gutter, three stops — the next match's opened to hold the kickoff, the rest collapsed to a line each — superseding [0086](./0086-week-widget-hero-and-rail.md)'s hero+rail and [0109](./0109-rail-solo-card.md)'s solo card, amending [0108](./0108-widgets-speak-the-day.md)'s MEDIUM parts (the small tile's spoken footer stands, so `kickoffDayName`/`kickoffDayDate` still travel). Snapshot v6 adds `kickoffDateLabel` (`12 SEP`, uppercase in BOTH languages — a stacked gutter is where caps are the design; one injection point in `formatWidgetKickoffParts`; optional in Swift, and a v5 file HIDES the date line — `kickoffDayDate` as fallback would say the weekday twice). The mock outranks the MD table where they disagree (its `1px` strokes are 0.5pt); the mock's opaque greys are transcribed WHITE-ALPHA over the plate or the accented tint flattens them all to full white (0114); the collapsed stop is a clear 5pt ring over a SPLIT rail (trap 60 — the mock's plate-filled dot inverts to a bright blob under tint); the spine's lime is one accent group and carries only WHICH STOP IS NEXT, never liveness (this tile still reads no live data); per-row `Link`s override the handoff's one-tap note (Ed's call). Row bands are GeometryReader arithmetic — hero 1.34 shares per collapsed row's 1, `layoutPriority` cannot express a ratio — with hairlines as overlays costing no height; the hero branches on measured band height (full form 44.0pt on the harness v bands of 46.9 standard / 45.7 mini / 42.9 SE / 40.1 zoomed SE — `compact` drops the 23pt time to 20 and the gap to 4 → 39.0); the hero pair is ONE row again (worst pair 160pt v a ~227pt SE column — 0086's stacking constraint died with the split panel); the 7pt collapsed date is the design's one step under the 8.5 floor; no TBC branch (`selectWidgetFixtures` drops `kickoffTbd` before the wire). `_debug/widgets` gains `&clock=12` on the `?sample=` links | Accepted — typechecked at every floor, harness-measured; simulator pass pending |
| [0128](./0128-widget-tiles-take-a-tray-shell.md) | The widget tiles take a TRAY SHELL — `MeshPlate` paints tray + 2pt-inset plate + lime corner glow in `.fullColor`, on ALL THREE widgets (Ed: one family, glow included; NEXT and NEWS are paint-only) — superseding [0126](./0126-widget-plate-goes-black.md)'s black and NARROWING [0125](./0125-widget-plate-goes-flat.md)'s "no more gradient" (aimed at the full-tile mesh; the one-corner glow is approved decoration). NO radius in the shell is ours (0085 §1): the tray's corner is the system's own container mask and every inner corner is `ContainerRelativeShape().inset(by: 2)` — concentric on every device without writing the handoff's 23/21. `Tok.plate` repoints to `#0b0d0f`; new `Tok.trayFill` `#17191b` is the mock's `white 5%` PRE-composited over the plate, because container alpha composites over BLACK (0114) and a literal `.05` wash renders `#0d0d0d`, erasing the step the tray exists to draw; `Tok.trayLine` white .09 at 0.5pt; the plate's lit top edge is `Tok.hairline` stroked on the inset shape and masked to a top fade so it follows the corner. The glow is the mock's `58% 78% at 100% −10% → clear 64%` via the 0104 recipe (trap 50 — `EllipticalGradient` takes no independent rx/ry), clipped to the plate, percentage-based so one shell fits small/medium/large; decoration only, never a data channel. Tinted/Clear/vibrant still get `Color.clear` (0114); 0125's system-sheen note stands | Accepted — typechecked at every floor; simulator pass pending |
| [0129](./0129-news-reel.md) | News becomes a FULL-SCREEN SNAP REEL (`handoff_newsreel/`) — one story per viewport (`FlatList` `pagingEnabled` + `snapToInterval` + `disableIntervalMomentum`, card height MEASURED by `onLayout` and gating first render), a pinned crown (`ReelCrown`, deliberately NOT the scrolls-away `templates/crown.tsx`) that after three device rounds paints the BOARD crown itself — `ReelBand`, CrownGrad verbatim at 340pt, `onCrown` ink, focused-gated dark status bar (`Stack.Screen statusBarStyle` red-boxes without an Info.plist key) — over cards whose `ReelWash` foot rises to full lime (Ed: "match our crown in the Board screen"; the dc dark veil and its white ink deleted), and a pull-down league filter (`ReelFilterPanel`) that is the attribution's ONLY home on the reel — superseding [0092](./0092-news-uniform-story-cards.md)'s News-screen half (its Today-card half and `NewsRow` stand). Real keyset paging replaces the prototype's `REEL_LAPS`: `useNewsReel` (`useInfiniteQuery`) chains `nextBefore`→`before`, SEEDS page one from the shared `useNews()` cache (widget/Today/link-sheet keys untouched), applies `selectReelItems` (unprintable rules WITHOUT the 48h cutoff — the reel pages back in time) and ends on a caught-up card; the link sheet reads the reel cache FIRST (a page-3 story exists nowhere else). Saves are URL-KEYED SNAPSHOTS (`SavedStory`, prefs v5 — ids purge at 30 days) with a Saved screen (`/news-saved`, `NewsRow` + new `accessory` un-save slot, snapshot mode on the link sheet) and `hapticSaveStory` on save-ON only. Deviations recorded: no tab bar under the cards, FLAT `reelGlass` fills instead of backdrop-blur (trap 59), 44pt circles over the reference's 40, native share sheet over the copy toast, the hint is TAP TO READ at 12pt (Ed's device feedback; the dc's 9.5 PULL UP promised a pull), NO crest (no club on the wire), the photo CONTAINED at its own aspect with edge fades into the ground (Ed 2026-09-07 — 16:9 thumbnails cover-cropped to 9:16 read too small; `ReelPhoto`, `reelDim` deleted), no bottom scrim, and every card grounded on `ReelWash` — a CrownGrad-family lime→teal ramp Ed picked on device ("similar to our crown"; `ReelFallbackPool` deleted) — which also serves the DESIGNED imageless card for the ~1-in-50 null / 404-able hot-linked photos | Presentation superseded by [0130](./0130-news-front-page-returns.md) — the front page returns; data layer, saves, Saved screen and story sheet stand |
| [0130](./0130-news-front-page-returns.md) | News FRONT PAGE returns; the reel retires after one day (Ed's mock: "go back to the old card style, but try this approach") — one `FlatList`: a HERO block (`NewsHero` tokens — the lead story's photo bleeds from behind the status bar under the white title, `dim` + a `fadeH` foot fade, the glass `NewsLeadCard` with topic pill · publisher · age / big headline / lime `Tap to read ›` overlapping by `overlap`), the day + `4 NEW` block (⚠ trap 41 returns VERBATIM — the count reads the lazy `seenAtOpen` capture, never the live preference the mount stamp overwrites) with a bookmark doorway to `/news-saved`, the neutral chip rail, then FLAT INFINITE rows (`NewsRow` restyled: title first at `headline`×2 lines, `NewsMeta` below with the topic as lime TEXT — the one pill per screen is the lead's) ending in caught-up + the standing attribution. Save moved INTO the story sheet (Share · Save · Close thirds; lime `primary` when saved; projections grew `imageUrl`/`isFirstParty`; ⚠ first-party stories open the browser directly and so have NO save affordance — accepted). SUPERSEDES [0129](./0129-news-reel.md)'s presentation only — its infinite keyset query (renamed `useNewsFeed`/`keys.newsFeed`), url-keyed snapshots, Saved screen and enriched sheet STAND. Deletion sweep grep-verified: 4 reel organisms, 2 molecules, `rise-glyph` + orphaned `share-glyph`, every `Reel*`/`reel*` token bar `Size.newsAction` (renamed), copy `reelRead`/`leagueFilter`; the reel-era dark status-bar flip removed, `MeshGround` back | Accepted — tsc + lint clean; simulator pass pending |
| [0131](./0131-one-display-voice-for-screen-titles.md) | Screen titles share ONE DISPLAY VOICE — SF Pro weight 300, tracking −0.04em of the size, three sizes only: `crownTitleLg` 48 (Clubs), `crownTitle` 40 (every other tab-level screen — the crown screens unchanged, News and Saved migrated), `heroTitle` 36 re-cut 700→300 (club hero, welcome statement). Stock-iOS `Type.largeTitle` (34/700) DELETED; section/sheet headings (`title`/`title3`) deliberately stay 700 — the weight contrast IS the hierarchy. Family stays SF Pro (0006/0014 hold); News' lighter 40pt title leans on `NewsHero.dim` for photo legibility | Accepted — tsc clean; simulator pass pending |
| [0132](./0132-team-windows-feed-the-today-board.md) | The Today board, reminders and widget snapshot merge each followed club's OWN `GET /cronogol/teams/{slug}/fixtures` window (−14d…+21d, INCLUSIVE bounds — trap 5) under the league-scoped window routes, because those structurally exclude cups, UEFA ties and segunda — Barcelona's UCL v Feyenoord (2026-09-09) lost NEXT UP to Levante (09-13), and five segunda clubs never appeared at all. New pure `team-window.ts`: club-perspective rows unfold to the neutral shape; sentinels `leagueSlug:''`/`season:0`/`matchweek:null`; opponent a slug-less `TeamRef` (join by id+slug only — 0022/0027 reaffirmed); `lastSyncedAt:null` → `[]` (trap 1); cup-DERBY halves complementary-merge so both slugs resolve (trap 49); dedupe-by-id, window WINS — pure-league output byte-identical (harnessed). `useTeamWindows` (`useQueries`+`combine`; structural sharing is LOAD-BEARING for the push-sync deps, trap 34) MAY refetch at kickoff — its window starts 14d back, so the 0052/0078 vanishing-fixture trap cannot bite; for a cup tie it is the only feed that flips `status`. ⚠ `sliceWindow` on `upcomingMine` is a bug guard: a past TBD row would sail through the `kickoffTbd ||` predicate into the deck as `--:--`. Non-league rows name their competition where league rows say `MD n` (Ed's pick); events disclosure OFF for them (`matchEventsCapable`) until the post-UCL-MD1 probe; widget `roundLabel` pill drops, slugs `''`→null on the wire, NO snapshot bump. Kicked-off cup ties hold the crown honestly; live polling untouched (sentinel keeps every live gate false) | Accepted — 14-assertion harness, tsc/lint/export clean, gallery + live simulator pass; ⚠ 09-09 kickoff sequence and the events probe pending |
| [0133](./0133-ucl-lockup-replaces-the-spelled-name.md) | The UCL LOCKUP replaces the spelled-out competition name on the NEXT UP and LAST RESULT cards (Ed supplied the SVG) — WHITE (`text`, the club names' ink), on NEXT UP riding the KICKOFF ROW's empty right end at 44pt (`competitionMarkLg`), on LAST RESULT in the meta row at 24 (placement+color Ed's, iterated across three screenshot rounds: head-row accent → under the Vs → kickoff row, bigger) — amends 0132 §9, whose verbatim "UEFA CHAMPIONS LEAGUE" truncated the venue and wrapped into the W pill; text stays as the FALLBACK for markless competitions (Copa del Rey…), never silence. BUNDLED, not backend-hosted (Ed's storage question answered): no wire field carries a competition asset, the master's navy fill is invisible on dark grounds while a drawn mark takes a theme token, and offline works day one — `logos/ucl-lockup.svg` design source, `atoms/competition-mark.tsx` the artwork as code (mark.tsx's "drawn, not loaded" precedent). Master's matrix transform BAKED into a measured 446×429 viewBox (⚠ path grammar: `.378.756` is TWO numbers — a naive regex shifts every coordinate after it); keyed by EXACT wire string (`competitionMarkKind` — display lookup, not an entity join; 0022/0027 stand); `Size.competitionMark` 24 (the lockup's wordmark is ~1/5 its height); VoiceOver keeps the words via accessibilityLabel. UPCOMING rows and the widget deliberately keep text/no pill | Accepted — tsc/lint baseline, live-data + gallery simulator pass |
| [0134](./0134-animated-splash-overlay.md) | The LAUNCH SPLASH is design's 2500ms handoff cycle (`handoff_splashscreen/SPLASH.md`) played by a JS OVERLAY (`templates/splash-overlay.tsx`) over a bare `#08090a` native ground — 0041's lockup-PNG splash retired (its hold + `.catch`es stand). Imperative shared values, one clock; the clip-path wipes are the overflow-hidden + COUNTER-TRANSLATE pair (pure transforms — width/height would re-layout the SVG sheet per frame); the collapse's surviving top edge IS the crown (`CrownGrad` stop 0). Every value in the `Splash` token group — the timeline lives THERE, not on `Motion` (`Deck.spring`'s reason). ARCHIVO 800/900 installed (OFL, static TTFs, config-plugin route — no `useFonts` gate): the app's only custom face, splash-only, the explicit 0131 exception; iOS resolves POSTSCRIPT names — a wrong one silently renders SF (`_debug/splash` is the check + replay harness). Ed's three calls: full splash ALWAYS (one code path; first runs double the mark — flagged to design), ready = SHELL RENDERED (no data gate — the spec's hold branch deliberately not built), Archivo over PNG/SF. RM variant: full sheet + settled lockup, 600ms hold, cross-fade, dark bar from frame 1 (light-on-lime is unreadable — spec deviation). Sweep rides `mixBlendMode: overlay` with a `SWEEP_BLEND` plain-sheen escape hatch. ⚠ `plugins/with-splash-storyboard-fix.js`: expo-splash-screen's NO-image path generates a broken storyboard (background never applied → #000000, dangling constraints, stale resource) — the local plugin repairs it and must sit BEFORE the package in the plugins array. ⚠ The Stack beneath is NOT animated — the spec's board fade first rode an `Animated.View` wrapper and `NativeTabs`' liquid-glass RAIL never attached under the animated ancestor (dock chromeless, Ed caught it; trap 64): the reveal is the overlay's own base-black dissolve, the 3.5% scale settle dropped. ⚠ The RM branch's timings carry `ReduceMotion.Never` — Reanimated's default SKIPS animations while the system switch is on, exactly when that branch runs (trap 63). Fade 250→120 (black-over-black, must clear before the 190ms strike) | Accepted — tsc/lint baseline; cinematic + RM verified by frame-stepped simulator recordings; dock rail verified restored after the trap-64 fix |
